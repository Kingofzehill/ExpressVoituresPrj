@model IEnumerable<ExpressVoitures.Models.Modele>

@{
    ViewData["Title"] = "Liste";
}

<h1>Liste des Modèles</h1>

<p>    
    <a asp-action="Create" class="btn btn-outline-primary btn-sm">Créer nouveau Modèle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marque)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LibelleModele)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marque.LibelleMarque)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LibelleModele)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info btn-sm">Détails</a>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-secondary btn-sm">Editer</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm">Supprimer</a>
            </td>
        </tr>
}
    </tbody>
</table>
